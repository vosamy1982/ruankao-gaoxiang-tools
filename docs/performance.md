# 前端性能基线

记录日期：2026-06-22

## 优化范围

- 顶层视图使用 `React.lazy` 和 `Suspense` 按 hash 路由加载。
- Markdown 管理后台仅在打开 `#admin` 时加载。
- 复习中心仅在打开 `#reviewCenter` 时加载，不进入考点阅览初始代码。
- 顶部导航改为下拉式入口，避免所有视图按钮同时挤在首屏。
- 后台编辑器使用无 Prism 高亮入口；右侧自定义预览仍支持 Markdown 和 GFM。
- 绩效域页面仅导入实际使用的 Lucide 图标，避免整个图标库进入产物。
- CI 对初始入口和任意 JavaScript 块执行体积门禁。

## 构建对比

| 指标 | v0.3.0 | v0.4.0 | v0.7.0 | v0.8.0 | v0.9.0 | v0.9.2 | v0.10.0 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 初始 JavaScript | 1,958.61 KB | 241.60 KB | 242.30 KB | 242.83 KB | 244.60 KB | 246.16 KB | 246.16 KB |
| 初始 JavaScript gzip | 605.12 KB | 75.63 KB | 75.83 KB | 76.24 KB | 76.64 KB | 77.04 KB | 77.05 KB |
| 最大按需块 | 1,958.61 KB | 281.46 KB | 281.49 KB | 281.46 KB | 281.45 KB | 281.45 KB | 281.45 KB |
| 复习中心按需块 | - | - | 7.92 KB | 7.89 KB | 7.89 KB | 7.89 KB | 12.48 KB |
| Vite 大块警告 | 有 | 无 | 无 | 无 | 无 | 无 | 无 |

初始 JavaScript 原始体积和 gzip 体积均减少约 87%。

## 自动门禁

先执行生产构建，再检查包体积：

```bash
npm run build
npm run bundle:check
```

当前阈值：

- 初始入口不超过 300 KiB。
- 任意 JavaScript 块不超过 500 KiB。

完整质量检查可使用 `npm run check`。
